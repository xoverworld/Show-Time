{#{% extends 'base.html.twig' %}#}

{#{% block title %}User Details{% endblock %}#}

{#{% block body %}#}
{#    <div class="container d-flex justify-content-center align-items-center mt-5">#}
{#        <div class="card bg-light shadow-lg rounded-4 w-100" style="max-width: 500px;">#}
{#            <div class="card-body">#}
{#                <h1 class="card-title text-center text-primary mb-4">User Details</h1>#}

{#                <ul class="list-group list-group-flush mb-4">#}
{#                    <li class="list-group-item"><strong>Email:</strong> {{ user.email }}</li>#}
{#                    {% if is_granted('ROLE_ADMIN') %}#}
{#                        <li class="list-group-item"><strong>Password:</strong> {{ user.password }}</li>#}
{#                        <li class="list-group-item"><strong>Role:</strong> {{ user.role }}</li>#}
{#                    {% endif %}#}
{#                    <li class="list-group-item"><strong>First Name:</strong> {{ user.userDetails.firstName }}</li>#}
{#                    <li class="list-group-item"><strong>Last Name:</strong> {{ user.userDetails.lastName }}</li>#}
{#                    <li class="list-group-item"><strong>Age:</strong> {{ user.userDetails.age }}</li>#}
{#                </ul>#}

{#                <div class="d-flex justify-content-center gap-3">#}
{#                    {% if is_granted('ROLE_ADMIN') %}#}
{#                        <a href="{{ path('user_update', {id: user.id}) }}" class="btn btn-outline-warning">Edit</a>#}
{#                    {% endif %}#}
{#                    <a href="{{ path('users') }}" class="btn btn-outline-secondary">Back</a>#}
{#                    {% if is_granted('ROLE_ADMIN') %}#}
{#                        <form action="{{ path('user_delete', {id: user.id}) }}" method="POST" onsubmit="return confirm('Are you sure you want to delete this user?');">#}
{#                            <button type="submit" class="btn btn-outline-danger">Delete</button>#}
{#                        </form>#}
{#                    {% endif %}#}
{#                </div>#}
{#            </div>#}
{#        </div>#}
{#    </div>#}
{#{% endblock %}#}


{% extends 'base.html.twig' %}

{% block title %}User Details{% endblock %}

{% block body %}
    <div class="container mt-5">
            <a href="{{ path('users') }}" class="btn btn-outline-light">
                &larr; Back to Users
            </a>
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="text-center mb-0 flex-grow-1">👤 User Details</h1>
        </div>

        <div class="card shadow-lg rounded-4 mx-auto" style="max-width: 600px;">
{#            <div class="card-header bg-transparent">#}
{#                <h2 class="card-title text-center text-primary mb-0">#}
{#                    {{ user.userDetails.firstName }} {{ user.userDetails.lastName }}#}
{#                </h2>#}
{#            </div>#}
            <div class="card-body">
                <div class="row mb-4">
                    <div class="col-md-6 text-center mb-3 mb-md-0">
                        <div class="bg-light rounded-circle d-inline-flex align-items-center justify-content-center"
                             style="width: 120px; height: 120px; background: var(--card-gradient);">
                            <span class="display-3">👤</span>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="d-flex align-items-center mb-2">
                            <span class="badge bg-primary rounded-pill me-2">ID</span>
                            <span>{{ user.id }}</span>
                        </div>
                        <div class="d-flex align-items-center mb-2">
                            <span class="badge bg-primary rounded-pill me-2">Age</span>
                            <span>{{ user.userDetails.age }}</span>
                        </div>
                    </div>
                </div>

                <div class="list-group">
                    <div class="list-group-item d-flex justify-content-between">
                        <span>First Name:</span>
                        <span class="fw-medium">{{ user.userDetails.firstName }}</span>
                    </div>
                    <div class="list-group-item d-flex justify-content-between">
                        <span>Last Name:</span>
                        <span class="fw-medium">{{ user.userDetails.lastName }}</span>
                    </div>
                    <div class="list-group-item d-flex justify-content-between">
                        <span>Email:</span>
                        <span class="fw-medium">{{ user.email }}</span>
                    </div>

                </div>
            </div>
            <div class="card-footer bg-transparent d-flex justify-content-center gap-3 py-3">
                <a href="{{ path('user_update', {id: user.id}) }}" class="btn btn-warning px-4">
                    ✏️ Edit
                </a>
                <a  href="{{ path('show-tickets', {id:user.id}) }}" class="btn btn-success px-4">
                    🎟️ Tickets
                </a>
                <form action="{{ path('user_delete', {id: user.id}) }}" method="POST"
                      onsubmit="return confirm('Are you sure you want to delete this user?');">
                    <button type="submit" class="btn btn-danger px-4">
                        🗑️ Delete
                    </button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
